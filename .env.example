# Copy this file to .env (and .env.local for secrets you don't want in
# version control) and fill in real values. Never commit the real .env file.

# --- Database (Neon Postgres) ---------------------------------------------
# Pooled connection string, used by the app at runtime.
DATABASE_URL="postgresql://neondb_owner:npg_f18aNpRzowrF@ep-icy-rice-aewv78i9-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
# Direct (non-pooled) connection string, used only by `prisma migrate`.
# On Neon this is the same host without the "-pooler" suffix.
DIRECT_URL="postgresql://neondb_owner:npg_f18aNpRzowrF@ep-icy-rice-aewv78i9.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require"

# --- Tracker auth ----------------------------------------------------------
# Shared secret between the VS Code extension and this API. Generate one with:
#   openssl rand -hex 32
TRACKER_API_TOKEN="8f77022422bd1f6dccde1577ea3de768ccaf1d4204c632d66bfff5e72fed06bd"

# --- Timezone ---------------------------------------------------------------
# IANA timezone used to compute "today", streaks, and weekly totals.
APP_TIMEZONE="Africa/Lagos"
